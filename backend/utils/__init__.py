"""
backend.utils — Shared utility functions and helpers.

Utility modules should be stateless, pure functions where possible.
They must not import from ``backend.services``, ``backend.repositories``,
or ``backend.agents`` to avoid circular dependencies.
"""
