"""
backend.api — FastAPI route handlers.

Routers are registered here and included in the main FastAPI application.
Each sub-module (e.g. ``auth``, ``products``, ``orders``) exposes an
``APIRouter`` that is imported and mounted in ``backend.main``.
"""
